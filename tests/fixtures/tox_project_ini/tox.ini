# A project dir with a tox.ini config.
[tox]
env_list = py312,lint

[testenv:py312]
deps = pytest>=8
    coverage
extras = dev
dependency_groups = test
commands = pytest tests
    coverage report

[testenv:lint]
deps = ruff
commands = ruff check .
